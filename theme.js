import { extendTheme } from '@chakra-ui/react'

const CustomizeTheme = extendTheme({

	colors: {
	   primary: "#cef",
	   secondary: "#ccc",
	   accent: "#fff",
	}

})


export default CustomizeTheme;
